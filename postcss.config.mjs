// Tailwind v4 + ui-kit theme tokens load through PostCSS to match the
// rest of the PolicyEngine Next.js portfolio.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
